# Render Blueprint for Separate Frontend Service
services:
  - type: web
    name: ethco-ai-frontend
    env: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    plan: free
    region: oregon
    branch: main
    routes:
      - type: rewrite
        source: /*
        destination: /index.html